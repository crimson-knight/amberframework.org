name: amberframework.org

server:
  host: 127.0.0.1
  port: 3000
  secret_key_base: amberframework-test

logging:
  severity: warn
  colorize: false
  filter:
    - password
    - confirm_password
  skip: []
  context:
    - request
    - session
    - headers
    - cookies
    - params
